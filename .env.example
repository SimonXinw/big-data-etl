# 复制本文件为项目根目录下的 `.env` 并填入真实值（`.env` 已被 .gitignore 忽略，勿提交仓库）。
# PostgreSQL / Supabase 连接串
# 例子：postgresql://postgres:password@localhost:5432/etl_learning
ETL_POSTGRES_DSN=

# Airflow 可选环境变量
ETL_TARGET=duckdb
ETL_DATA_SOURCE=csv
ETL_PROJECT_ROOT=.

# —— 全店订单 / 大数据 ETL：请用下面 Admin 凭据（不需要、也不应走「顾客登录」那套）——
# SHOPIFY_STORE_DOMAIN=your-store.myshopify.com
# SHOPIFY_ADMIN_ACCESS_TOKEN=shpat_xxx
# 后台自定义应用需勾选 read_orders（看 60 天前全量单可再勾 read_all_orders）、read_customers 等 scope。
#
# NEXT_PUBLIC_CUSTOMER_ACCOUNT_SHOP_ID / NEXT_PUBLIC_CUSTOMER_ACCOUNT_CLIENT_ID（若你有）：
# 仅用于 Hydrogen / 店面「顾客账号」OAuth，拿到的是某位登录顾客的访问令牌；
# 不能替代 Admin 用来拉「整店」订单数据集，本项目 ETL 也不读取这两项。
# Storefront API（`python -m etl_project --probe-storefront-products` 店面商品探测）
# SHOPIFY_STOREFRONT_ACCESS_TOKEN=
# 可选：店面专用 API 版本（默认先读此项，否则沿用 SHOPIFY_API_VERSION，再默认 2025-10）
# SHOPIFY_STOREFRONT_API_VERSION=2025-10
# 兼容旧名：SHOPIFY_ACCESS_TOKEN=shpat_xxx
# Admin GraphQL 路径为 /admin/api/{version}/graphql.json；版本优先级：SHOPIFY_ADMIN_API_VERSION > SHOPIFY_API_VERSION > 默认 2025-04
# shopify.dev 文档示例常用较新版本（如 2026-04）；若拉订单失败且怀疑版本过旧，可单独提高 Admin 版本：
# SHOPIFY_ADMIN_API_VERSION=2026-04
# 默认不写则代码内为 2025-04（本仓库订单增量同步默认对齐该版本）。
# —— Shopify 订单：按 UTC 自然日回溯 N 天，每日单独查询该日 updated_at 窗口 ——
# ETL_SHOPIFY_SYNC_DAYS=14
# 订单 JSON 固定写入 data/orders/shopify_orders_snapshot.json；默认开启，设为 0 关闭： ETL_SHOPIFY_JSON_SNAPSHOT_ENABLE=1
# ETL_SHOPIFY_SHOP_NAME=AWOL-US
#（不设则用 SHOPIFY_STORE_DOMAIN 子域作为店铺展示名）
